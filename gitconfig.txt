git config --global user.name abruzzisz


  
-- Criar um projeto React Native Expo
npx create-expo-app@latest calculadora --template blank

-- Resturar o projeto (Toda vez que fiz um clone)
cd calculadora
npm i

 -- Executar o projeto
 npx expo start
 npx expo start --android